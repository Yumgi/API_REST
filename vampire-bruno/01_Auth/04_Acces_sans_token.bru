meta {
  name: 04 Acces sans token
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/vms
  body: none
  auth: none
}

tests {
  test("Doit refuser avec 401", function() {
    expect(res.status).to.equal(401);
  });
}
