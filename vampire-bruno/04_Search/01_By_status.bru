meta {
  name: 01 Recherche par status
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/vms/search?status=running
  body: none
  auth: none
}

params:query {
  status: running
}

headers {
  Authorization: Bearer {{token}}
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}
